;; Stub — LinkedSet requires IPersistentSet + Sequential which babashka
;; doesn't support yet. This file exists so that (require 'linked.core)
;; succeeds; it simply doesn't define any set types.
(ns linked.set)
